name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1003184@ds.study.iitm.ac.in
        run: echo "Hello, world!"
```

6. **Commit changes** click kar
7. **Actions** tab pe ja → **"Run workflow"** click kar

Phir submit karna:
```
https://github.com/CipherGaurav/CipherGaurav.github.io
